# Project rules and notes for AI-assisted coding

1. Forms should use `react-hook-form` with schema validation (zod) for production code.
2. All interactive components must include accessible labels and ARIA where appropriate.
3. Write unit tests for validation logic and accessible behavior before merging.

Update this file with concrete rules learned from each exercise.
